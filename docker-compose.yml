version: "2"

services:
  db:
    image: mysql:latest
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: sunny@123
      MYSQL_DATABASE: omeka
      MYSQL_USER: root
      MYSQL_PASSWORD: sunny@123
    # volumes:
    # mysql-data:
    #     driver: local

  db_admin:
    image: phpmyadmin/phpmyadmin:latest
    restart: always
    links:
      - db:db
    ports:
      - "8081:80"

  omeka-classic:
    image: sunny3p/omeka-classic:latest
    # links:
    # - mysql
    restart: always
    volumes:
      - ./app:/var/www/html/files
    ports:
      - 8080:80
